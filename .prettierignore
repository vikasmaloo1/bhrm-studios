# Build outputs
.next/
out/
build/
dist/

# Dependencies
node_modules/
.pnpm-store/

# Test + tooling artifacts (not source)
test_reports/

# Lock files (managed by pnpm)
pnpm-lock.yaml

# Generated files
next-env.d.ts
*.tsbuildinfo

# Public assets
# Binary files (images, fonts, favicons) are excluded.
# Text-based assets (SVG, JSON manifests) in public/ are intentionally
# NOT excluded so Prettier can format them if needed.
public/**/*.ico
public/**/*.png
public/**/*.jpg
public/**/*.jpeg
public/**/*.gif
public/**/*.woff
public/**/*.woff2
public/**/*.ttf
public/**/*.eot